import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('LocalSupportPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CommitmentTermPage(title: 'CommitmentTerm')));
    //  final titleFinder = find.text('CommitmentTerm');
    //  expect(titleFinder, findsOneWidget);
  });
}
