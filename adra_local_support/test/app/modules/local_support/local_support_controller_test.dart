import 'package:adra_local_support/adra_local_support.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  initModule(LocalSupportModule());
  // CommitmentTermController commitmentterm;
  //
  setUp(() {
    //     commitmentterm = CommitmentTermModule.to.get<CommitmentTermController>();
  });

  group('LocalSupportController Test', () {
    //   test("First Test", () {
    //     expect(commitmentterm, isInstanceOf<CommitmentTermController>());
    //   });

    //   test("Set Value", () {
    //     expect(commitmentterm.value, equals(0));
    //     commitmentterm.increment();
    //     expect(commitmentterm.value, equals(1));
    //   });
  });
}
